import { ProbabilityPercentagePipe } from '../probability-percentage.pipe';

describe('ProbabilityPercentagePipe', () => {
  it('create an instance', () => {
    const pipe = new ProbabilityPercentagePipe();
    expect(pipe).toBeTruthy();
  });
});
